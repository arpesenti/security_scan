The `pi` CLI tool responds slowly, causing subprocess-based CLI tests to timeout with 30s timeout; mocked tests run in ~0.02 seconds.	TASK_0002
python3 is not installed on this machine; Python syntax validation and pytest smoke tests cannot run (UNOBSERVED for those specific checks, but not relevant to the markdown report deliverable)	TASK_0004
